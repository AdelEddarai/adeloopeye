import { MarketsContent } from '@/features/markets/components/MarketsContent';

export const metadata = {
  title: 'Live Markets — Adeloopeye',
  description: 'Real-time global, commodities, economic and Morocco market data',
};

export default function MarketsPage() {
  return <MarketsContent />;
}
