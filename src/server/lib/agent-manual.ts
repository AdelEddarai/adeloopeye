/**
 * Thin compatibility wrapper.
 * The actual doctrine now lives in adeloopeye-doctrine.ts.
 */

export type { LiveDoctrineContext as LiveContext } from './adeloopeye-doctrine';
export { buildAdeloopeyeInstructionsMarkdown as buildAgentManual } from './adeloopeye-doctrine';
